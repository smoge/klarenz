# klarenz

## Execute

* Run `stack exec -- klarenz-exe` to see "We're inside the application!"
* With `stack exec -- klarenz-exe --verbose` you will see the same message, with more logging.

## Run tests

`stack test`
